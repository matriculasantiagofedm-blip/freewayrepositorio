rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // DESBLOQUEO TOTAL: Acceso universal para evitar errores de permisos en operación multi-usuario e impresión.
      allow read, write: if true;
    }
  }
}