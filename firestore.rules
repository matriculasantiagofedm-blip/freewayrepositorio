rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Reglas totalmente abiertas para desarrollo y prototipado
      // Permite lectura y escritura sin restricciones para evitar errores de permisos en impresión
      allow read, write: if true;
    }
  }
}