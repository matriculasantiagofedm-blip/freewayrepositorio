rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // DESBLOQUEO TOTAL: Acceso universal para facilitar la operación multi-rol
      allow read, write: if true;
    }
  }
}