rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Reglas totalmente abiertas para garantizar que la generación de certificados 
    // y las vistas de impresión funcionen sin errores de permisos.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}