rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // ACCESO TOTAL: Permitir lectura y escritura universal para facilitar la operación multi-rol y evitar errores de permisos.
      allow read, write: if true;
    }
  }
}